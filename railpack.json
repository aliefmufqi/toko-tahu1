{
  "$schema": "https://schema.railpack.com",
  "provider": "php",
  "packages": {
    "php": "8.3"
  },
  "deploy": {
    "startCommand": "php artisan storage:link --quiet && php artisan migrate --force --quiet && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
